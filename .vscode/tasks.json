{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Odin",
            "type": "shell",
            "command": "odin build . -out:build/athir.exe; xcopy /e /i /y \"assets\" \"build\\assets\"; start build/athir.exe",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
