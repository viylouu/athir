{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Odin",
            "type": "shell",
            "command": "odin build .; xcopy assets build\\assets /s /e /y; mv athir.exe build/athir.exe; start build/athir.exe",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
